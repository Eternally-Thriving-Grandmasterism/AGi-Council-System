### Repo Deploy: Add New File `pyproject.toml` (Ruff Config Mercy)
[tool.ruff]
select = ["E", "F", "W", "I", "N", "B", "A", "C4", "UP"]  # Broad lint grace
ignore = ["E501"]  # Line length mercy (adjust)

target-version = "py310"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.per-file-ignores]
"tests/*" = ["S101"]  # Assert mercy in tests
